import { GraphQLField, GraphQLSchema } from 'graphql-tools/dist/transforms/node_modules/graphql';
import { Transform } from 'graphql-tools/dist/transforms/transforms';
export default class RenameRootFields implements Transform {
    private transformer;
    constructor(renamer: (operation: 'Query' | 'Mutation' | 'Subscription', name: string, field: GraphQLField<any, any>) => string);
    transformSchema(originalSchema: GraphQLSchema): GraphQLSchema;
}
