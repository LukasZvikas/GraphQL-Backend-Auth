import { GraphQLSchema } from 'graphql-tools/dist/transforms/node_modules/graphql';
import { Request } from 'graphql-tools/dist/Interfaces';
import { Transform } from 'graphql-tools/dist/transforms/transforms';
export default class AddArgumentsAsVariablesTransform implements Transform {
    private schema;
    private args;
    constructor(schema: GraphQLSchema, args: {
        [key: string]: any;
    });
    transformRequest(originalRequest: Request): Request;
}
