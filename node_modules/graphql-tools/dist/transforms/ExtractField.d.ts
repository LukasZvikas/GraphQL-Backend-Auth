import { Transform, Request } from 'graphql-tools/dist/Interfaces';
export default class ExtractField implements Transform {
    private from;
    private to;
    constructor({ from, to }: {
        from: Array<string>;
        to: Array<string>;
    });
    transformRequest(originalRequest: Request): Request;
}
