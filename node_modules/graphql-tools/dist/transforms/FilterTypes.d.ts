import { GraphQLSchema, GraphQLNamedType } from 'graphql-tools/dist/transforms/node_modules/graphql';
import { Transform } from 'graphql-tools/dist/transforms/transforms';
export default class FilterTypes implements Transform {
    private filter;
    constructor(filter: (type: GraphQLNamedType) => boolean);
    transformSchema(schema: GraphQLSchema): GraphQLSchema;
}
